// Shared with the extension, which carries a withdrawn translator's languages
// over to its replacement by the same rule (see `ReplacementTranslator`).
export * from "@extension/common/translators/findClosestLanguage.ts";
